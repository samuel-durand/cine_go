# Configuration Nixpacks pour le serveur
# Ce fichier force Nixpacks à installer les dépendances Node.js

[phases.setup]
nixPkgs = ["nodejs-18_x", "npm"]

[phases.install]
cmds = ["npm install"]

[start]
cmd = "npm start"

